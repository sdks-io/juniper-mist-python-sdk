# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.port_config_1 import PortConfig1


class Rules6(object):

    """Implementation of the 'Rules6' model.

    TODO: type model description here.

    Attributes:
        additional_property (string): Property key is `match_name[x:y]`, where
            x and y are the first and the last characters of the string to
            match. Value is the string to match.
        match_model (string): TODO: type description here.
        name (string): TODO: type description here.
        port_config (dict): Object key is a comma separated list of interface
            names or interface ranges (e.g. ge-0/0/0, ge-0/0/0-5)

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "additional_property": 'additionalProperty',
        "match_model": 'match_model',
        "name": 'name',
        "port_config": 'port_config'
    }

    _optionals = [
        'additional_property',
        'match_model',
        'name',
        'port_config',
    ]

    def __init__(self,
                 additional_property=APIHelper.SKIP,
                 match_model=APIHelper.SKIP,
                 name=APIHelper.SKIP,
                 port_config=APIHelper.SKIP):
        """Constructor for the Rules6 class"""

        # Initialize members of the class
        if additional_property is not APIHelper.SKIP:
            self.additional_property = additional_property 
        if match_model is not APIHelper.SKIP:
            self.match_model = match_model 
        if name is not APIHelper.SKIP:
            self.name = name 
        if port_config is not APIHelper.SKIP:
            self.port_config = port_config 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        additional_property = dictionary.get("additionalProperty") if dictionary.get("additionalProperty") else APIHelper.SKIP
        match_model = dictionary.get("match_model") if dictionary.get("match_model") else APIHelper.SKIP
        name = dictionary.get("name") if dictionary.get("name") else APIHelper.SKIP
        port_config = PortConfig1.from_dictionary(dictionary.get('port_config')) if 'port_config' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(additional_property,
                   match_model,
                   name,
                   port_config)
