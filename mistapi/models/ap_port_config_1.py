# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.ap_port_config import ApPortConfig


class ApPortConfig1(object):

    """Implementation of the 'ApPortConfig1' model.

    TODO: type model description here.

    Attributes:
        model_specific (dict): the property key is the AP model (e.g "AP32")

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "model_specific": 'model_specific'
    }

    _optionals = [
        'model_specific',
    ]

    def __init__(self,
                 model_specific=APIHelper.SKIP):
        """Constructor for the ApPortConfig1 class"""

        # Initialize members of the class
        if model_specific is not APIHelper.SKIP:
            self.model_specific = model_specific 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        model_specific = ApPortConfig.from_dictionary(dictionary.get('model_specific')) if 'model_specific' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(model_specific)
