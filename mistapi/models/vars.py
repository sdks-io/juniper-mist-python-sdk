# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper


class Vars(object):

    """Implementation of the 'Vars' model.

    TODO: type model description here.

    Attributes:
        ssid_str (string): TODO: type description here.
        vlan_id (string): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "ssid_str": 'SSID_STR',
        "vlan_id": 'VLAN_ID'
    }

    _optionals = [
        'ssid_str',
        'vlan_id',
    ]

    def __init__(self,
                 ssid_str=APIHelper.SKIP,
                 vlan_id=APIHelper.SKIP):
        """Constructor for the Vars class"""

        # Initialize members of the class
        if ssid_str is not APIHelper.SKIP:
            self.ssid_str = ssid_str 
        if vlan_id is not APIHelper.SKIP:
            self.vlan_id = vlan_id 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        ssid_str = dictionary.get("SSID_STR") if dictionary.get("SSID_STR") else APIHelper.SKIP
        vlan_id = dictionary.get("VLAN_ID") if dictionary.get("VLAN_ID") else APIHelper.SKIP
        # Return an object of this model
        return cls(ssid_str,
                   vlan_id)
