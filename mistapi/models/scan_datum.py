# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class ScanDatum(object):

    """Implementation of the 'ScanDatum' model.

    TODO: type model description here.

    Attributes:
        ap (string): mac address of the AP associated with the BSSID scanned
        band (Band12Enum): 5GHz or 2.4GHz band, associated with the BSSID
            scanned
        bssid (string): BSSID found during client’s background scan for wifi
        channel (int): channel of the band found in the scan
        rssi (float): client’s RSSI relative to the BSSID scanned
        ssid (string): ESSID containing the BSSID scanned
        timestamp (float): time the scan of the particular BSSID occurred

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "ap": 'ap',
        "band": 'band',
        "bssid": 'bssid',
        "channel": 'channel',
        "rssi": 'rssi',
        "ssid": 'ssid',
        "timestamp": 'timestamp'
    }

    def __init__(self,
                 ap=None,
                 band=None,
                 bssid=None,
                 channel=None,
                 rssi=None,
                 ssid=None,
                 timestamp=None):
        """Constructor for the ScanDatum class"""

        # Initialize members of the class
        self.ap = ap 
        self.band = band 
        self.bssid = bssid 
        self.channel = channel 
        self.rssi = rssi 
        self.ssid = ssid 
        self.timestamp = timestamp 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        ap = dictionary.get("ap") if dictionary.get("ap") else None
        band = dictionary.get("band") if dictionary.get("band") else None
        bssid = dictionary.get("bssid") if dictionary.get("bssid") else None
        channel = dictionary.get("channel") if dictionary.get("channel") else None
        rssi = dictionary.get("rssi") if dictionary.get("rssi") else None
        ssid = dictionary.get("ssid") if dictionary.get("ssid") else None
        timestamp = dictionary.get("timestamp") if dictionary.get("timestamp") else None
        # Return an object of this model
        return cls(ap,
                   band,
                   bssid,
                   channel,
                   rssi,
                   ssid,
                   timestamp)
