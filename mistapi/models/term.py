# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.action_3 import Action3
from mistapi.models.matching_1 import Matching1


class Term(object):

    """Implementation of the 'Term' model.

    TODO: type model description here.

    Attributes:
        action (Action3): when used as import policy
        matching (Matching1): zero or more criteria/filter can be specified to
            match the term, all criteria have to be met

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "action": 'action',
        "matching": 'matching'
    }

    _optionals = [
        'action',
        'matching',
    ]

    def __init__(self,
                 action=APIHelper.SKIP,
                 matching=APIHelper.SKIP):
        """Constructor for the Term class"""

        # Initialize members of the class
        if action is not APIHelper.SKIP:
            self.action = action 
        if matching is not APIHelper.SKIP:
            self.matching = matching 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        action = Action3.from_dictionary(dictionary.get('action')) if 'action' in dictionary.keys() else APIHelper.SKIP
        matching = Matching1.from_dictionary(dictionary.get('matching')) if 'matching' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(action,
                   matching)
