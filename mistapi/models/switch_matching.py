# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.rules_6 import Rules6


class SwitchMatching(object):

    """Implementation of the 'SwitchMatching' model.

    TODO: type model description here.

    Attributes:
        additional_config_cmds (list of string): additional CLI commands to
            append if this rule matches NOTE: no check is done
        enabled (bool): TODO: type description here.
        rules (list of Rules6): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "additional_config_cmds": 'additional_config_cmds',
        "enabled": 'enabled',
        "rules": 'rules'
    }

    _optionals = [
        'additional_config_cmds',
        'enabled',
        'rules',
    ]

    def __init__(self,
                 additional_config_cmds=APIHelper.SKIP,
                 enabled=False,
                 rules=APIHelper.SKIP):
        """Constructor for the SwitchMatching class"""

        # Initialize members of the class
        if additional_config_cmds is not APIHelper.SKIP:
            self.additional_config_cmds = additional_config_cmds 
        self.enabled = enabled 
        if rules is not APIHelper.SKIP:
            self.rules = rules 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        additional_config_cmds = dictionary.get("additional_config_cmds") if dictionary.get("additional_config_cmds") else APIHelper.SKIP
        enabled = dictionary.get("enabled") if dictionary.get("enabled") else False
        rules = None
        if dictionary.get('rules') is not None:
            rules = [Rules6.from_dictionary(x) for x in dictionary.get('rules')]
        else:
            rules = APIHelper.SKIP
        # Return an object of this model
        return cls(additional_config_cmds,
                   enabled,
                   rules)
