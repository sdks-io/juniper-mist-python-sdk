# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.delivery import Delivery


class Rules(object):

    """Implementation of the 'Rules' model.

    TODO: type model description here.

    Attributes:
        delivery (Delivery): Delivery object to configure the alarm delivery
        enabled (bool): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "delivery": 'delivery',
        "enabled": 'enabled'
    }

    _optionals = [
        'delivery',
        'enabled',
    ]

    def __init__(self,
                 delivery=APIHelper.SKIP,
                 enabled=APIHelper.SKIP):
        """Constructor for the Rules class"""

        # Initialize members of the class
        if delivery is not APIHelper.SKIP:
            self.delivery = delivery 
        if enabled is not APIHelper.SKIP:
            self.enabled = enabled 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        delivery = Delivery.from_dictionary(dictionary.get('delivery')) if 'delivery' in dictionary.keys() else APIHelper.SKIP
        enabled = dictionary.get("enabled") if "enabled" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(delivery,
                   enabled)
