# -*- coding: utf-8 -*-

"""
mistapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from mistapi.api_helper import APIHelper
from mistapi.models.after import After
from mistapi.models.before import Before


class Result9(object):

    """Implementation of the 'Result9' model.

    TODO: type model description here.

    Attributes:
        admin_id (uuid|string): TODO: type description here.
        admin_name (string): TODO: type description here.
        after (After): field values after the change
        before (Before): field values prior to the change
        for_site (bool): TODO: type description here.
        id (uuid|string): TODO: type description here.
        message (string): TODO: type description here.
        org_id (uuid|string): TODO: type description here.
        site_id (uuid|string): TODO: type description here.
        timestamp (float): TODO: type description here.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "admin_id": 'admin_id',
        "admin_name": 'admin_name',
        "id": 'id',
        "message": 'message',
        "org_id": 'org_id',
        "site_id": 'site_id',
        "timestamp": 'timestamp',
        "after": 'after',
        "before": 'before',
        "for_site": 'for_site'
    }

    _optionals = [
        'after',
        'before',
        'for_site',
    ]

    def __init__(self,
                 admin_id=None,
                 admin_name=None,
                 id=None,
                 message=None,
                 org_id=None,
                 site_id=None,
                 timestamp=None,
                 after=APIHelper.SKIP,
                 before=APIHelper.SKIP,
                 for_site=APIHelper.SKIP):
        """Constructor for the Result9 class"""

        # Initialize members of the class
        self.admin_id = admin_id 
        self.admin_name = admin_name 
        if after is not APIHelper.SKIP:
            self.after = after 
        if before is not APIHelper.SKIP:
            self.before = before 
        if for_site is not APIHelper.SKIP:
            self.for_site = for_site 
        self.id = id 
        self.message = message 
        self.org_id = org_id 
        self.site_id = site_id 
        self.timestamp = timestamp 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if dictionary is None:
            return None

        # Extract variables from the dictionary

        admin_id = dictionary.get("admin_id") if dictionary.get("admin_id") else None
        admin_name = dictionary.get("admin_name") if dictionary.get("admin_name") else None
        id = dictionary.get("id") if dictionary.get("id") else None
        message = dictionary.get("message") if dictionary.get("message") else None
        org_id = dictionary.get("org_id") if dictionary.get("org_id") else None
        site_id = dictionary.get("site_id") if dictionary.get("site_id") else None
        timestamp = dictionary.get("timestamp") if dictionary.get("timestamp") else None
        after = After.from_dictionary(dictionary.get('after')) if 'after' in dictionary.keys() else APIHelper.SKIP
        before = Before.from_dictionary(dictionary.get('before')) if 'before' in dictionary.keys() else APIHelper.SKIP
        for_site = dictionary.get("for_site") if "for_site" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(admin_id,
                   admin_name,
                   id,
                   message,
                   org_id,
                   site_id,
                   timestamp,
                   after,
                   before,
                   for_site)
